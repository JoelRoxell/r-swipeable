import React, {Component, PropTypes} from 'react';
import ReactDOM from 'react-dom';

import SwipeableApp from './swipeable-app';

ReactDOM.render(<SwipeableApp />, document.getElementById('swipeable-app'));
